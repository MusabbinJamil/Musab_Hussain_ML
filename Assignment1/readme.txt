This repository contains ML predictions and results for 5 datasets from UCI ML repository. The dataset links can be found inside the dashboard. We wanted to deploy the dashbboard on PythonAnyWhere but we used dash_bootstrap_components, which we could not install on the server console. Which lead to code rendereing issues. Nonetheless the user can run the app.py file given the requirements.txt is setup with python